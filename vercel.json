{
  "builds": [
    {
      "src": "src/app.js",
      "use": "@vercel/node"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "hooks": {
    "build": "npm install --only=dev && npm run build"
  }
}
